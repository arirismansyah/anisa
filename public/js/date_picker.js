$(".year-picker").bootstrapdatepicker({
    format: "yyyy",
    viewMode: "year",
    minViewMode: "years",
});

$('.month-picker').bootstrapdatepicker({
    format: "m",
    viewMode: "months",
    minViewMode: "months",
});

$('.date-picker').bootstrapdatepicker({
    format: "dd-mm-yyyy",
    viewMode: "date",
})